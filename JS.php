<!DOCTYPE html>
<html lang="en">
<head>
    <title>JS Lab Class</title>
</head>
<body>
    <h1>This is JS 1st class</h1>

    <script>
        // ✅ 1. 'document' was misspelled as 'docoment'
        document.write("Hello World<br>");

        // ✅ 2. 'alart' should be 'alert'
        console.log("hello class");
        alert("Hello Bangladesh");

        console.log("Hello world, I am from console");

        // ✅ 3. Variable declarations
        var name = "Ashraful";
        var age = 20;
        var is_human = true;
        var x;

        // ✅ 4. 'human' was not defined; should be 'is_human'
        console.log("The type of name:", typeof name);
        console.log("The type of age:", typeof age);
        console.log("The type of is_human:", typeof is_human);
        console.log("The type of x:", typeof x);

        // ✅ 5. Output variables
        console.log("The name is:", name);

        var num_1 = 20;
        var num_2 = 50;
        var sum = num_1 + num_2;
        var div = num_1 / num_2;

        console.log("The sum is:", sum);
        console.log("The division is:", div);
        console.log("The subtraction is:", num_1 - num_2);
    </script>
</body>
</html>
